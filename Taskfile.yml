version: "3"

tasks:
  run:
    env:
      config: docker
    cmds:
      - go run ./cmd/auth/main.go

  proto:
    cmds:
      - protoc --go_out=./proto --go-grpc_out=./proto --proto_path=./proto ./proto/*.proto

  tidy:
    cmds:
      - go mod tidy

  test:
    cmds:
      - go test ./...
